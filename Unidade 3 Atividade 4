#include <stdlib.h>
#include <stdio.h>
#include <locale.h>
int main(){
	setlocale (LC_ALL,"");
	int i,soma;
	soma=0;
	for (i=200 ; i<=500 ; i++){
		if(i % 2 != 0){
			soma = soma + i;		
			}
	}
	printf("soma de todos os numero impares é: %d",soma);
return (0);
}
