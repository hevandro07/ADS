#include <stdio.h> 
int main()
{
 char nome[50];
 printf("Digite seu nome:");
 scanf("%s",&nome);
 printf("\nBem vindo(a) a disciplina de Algoritimos e logica de programacao II,%s.",&nome);
 return (0);
}
