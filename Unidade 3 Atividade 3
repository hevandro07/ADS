#include <stdio.h>
#include <stdlib.h>
int main(){
	int i, j, soma;
	for(i=1 ; i<=10 ; i++){
		printf("\nTabuada do %d\n",i);
		for(j=1 ; j<=10 ; j++){
		soma= i * j;
		printf("%dX%d= %d\n",i,j,soma);
	}
	}
	return (0);
}
